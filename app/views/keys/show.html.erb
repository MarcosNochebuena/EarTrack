<p style="color: green">
  <%= notice %>
</p>

<div class="container">
  <div class="card">
    <div class="card-header bg-dark d-flex justify-content-between align-items-center">
      <%= link_to "Atras" , keys_path, class: "btn btn-secondary" %>
      <h1 class="text-white text-center mx-auto">Datos de la clave: <%= @key.num_key %></h1>
      <%= link_to "Editar Clave", edit_key_path(@key), class: 'btn btn-success' %>
    </div>
    
    <div class="card-body">
      <table class="table table-bordered">
        <tbody>
          <tr>
            <th class="col-3">Clave: </th>
            <td>
              <%= @key.num_key %>
            </td>
          </tr>
          <tr>
            <th class="col-3">Clave UPP: </th>
            <td>
              <%= @key.upp %>
            </td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>
</div>