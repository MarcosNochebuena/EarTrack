<div id="layoutAuthentication">
  <div id="layoutAuthentication_content">
    <main>
      <div class="container">
        <div class="row justify-content-center">
          <div class="col-lg-9">
            <div class="card shadow-lg border-0 rounded-lg mt-5">
              <div class="card-header"><h3 class="text-center font-weight-light my-4"><%= t('common.password_recovery') %></h3></div>
              <div class="card-body">
                <div class="small mb-3 text-muted"><%= t('common.password_recovery_description') %></div>
                <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
                  <%= render "devise/shared/error_messages", resource: resource %>

                  <div class="form-floating">
                    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
                    <%= f.label :email %><br />
                  </div>
                  <div class="d-flex justify-content-end">
                    <%= f.submit t('common.send_reset_instructions'), class: "btn btn-primary btn-lg px-4 py-2" %>
                  </div>
                <% end %>
              </div>
              <div class="card-footer text-center py-3">
                <%= render "devise/shared/links" %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </main>
  </div>
</div>
